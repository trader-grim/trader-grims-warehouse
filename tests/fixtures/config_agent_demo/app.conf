# Demo application config — todo-2016 config-agent fixture ONLY.
# Never a real host config path; safe by construction for the first live run.
[server]
host = 127.0.0.1
port = 8080
workers = 2

[logging]
level = info
output = stdout
