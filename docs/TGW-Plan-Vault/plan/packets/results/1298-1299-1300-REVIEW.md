Status: cleared
Reviewer: Claude (runner-review)
Todos: #1298, #1299, #1300 (bundled — shared root file api.py, shared
root-cause class for #1298/#1299)   PP: PP-COHESION-001
Checked: diff (`git diff f219b4b todo/1298-1299-1300-api-fence-writes`)
against all three todo briefs, scope (api.py + 2 test files only),
result manifest completeness.
Summary:
- #1298 — all 7 named call sites (catalog-verify --mark-verified, cmd_hint,
  legacy-listing resolve, freeship-apply, set-template, quality --save,
  lookup --save) now pass `archive_root=cfg.get('archive_root')`. Confirmed
  each site individually against the diff.
- #1299 — migrate-unblock's destructive field-removal write now passes
  archive_root too.
- #1300 — migrate-restore now checks `dest.exists()` before writing and
  refuses (prints a clear message, returns 1) unless a new subcommand-scoped
  `--force` flag is passed; the dry_run short-circuit still fires before any
  write attempt (order preserved: parent-exists check → new exists-check →
  dry_run check → write). Write also now passes archive_root.
Test coverage is strong: cmd_hint's archive_root kwarg is spied directly;
migrate-restore has 3 tests covering refuse-without-force (live item
untouched), force-overwrite (archive_root passed), and succeed-when-absent.
Full suite: 2140 passed, 1 skipped, 1 pre-existing-and-independently-verified
flake (#1370, same root cause confirmed by both this and the #1306 branch —
reproduces on bare pre-batch commit f219b4b in isolation, unrelated to any
of these 5 branches). No deviations, no new out-of-scope todos (the one
adjacent finding was already tracked as #1370). No triggers fired. Cleared
for stitch.
