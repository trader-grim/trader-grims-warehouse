# Review: 1278+1279 pm-intake-fetch-url-ssrf-sizecap
Status: cleared — concurrent batch (post 2-in-a-row graduation), stitching independently.
Reviewer: Claude (main session, tgw-runner-review)

Checked: Spec — `_resolve_is_safe()` matches the packet's exact
implementation; `_SSRFBlocked`/`_ssrf_guard_hook` correctly wired both as
a pre-flight check (before the client is created) and via
`event_hooks={'request': [...]}` so every redirect hop is re-checked, per
spec. Streaming body read replaces the buffering `client.get()`, with a
`Content-Length` fast-path reject plus incremental `iter_bytes()`
accumulation aborted at `_MAX_RESPONSE_BYTES` mid-stream, matching spec.
Encoding handled via `resp.encoding or 'utf-8'` on the streamed response,
as specced. Out-of-scope — only `pm_intake.py`'s `fetch_url()` and its
new/updated tests touched; `_URL_FETCH_MAX_CHARS`, note-extraction, and
LLM classification untouched.

Live evidence — re-verified independently: `tgw.workers.pm_intake.__file__`
resolves under the worktree path; full offline suite 2126 passed/1
skipped/0 failed (Postgres peer-auth warning in output is an unrelated
pre-existing migration-skip path when running as `db`, not a regression).
All 7 acceptance scenarios present and go beyond the letter of the spec:
notably the redirect test (acceptance #3) uses a REAL `httpx.Client` +
`httpx.MockTransport` (only the transport layer is mocked) rather than
mocking `fetch_url`'s own logic, which is what actually proves the
`event_hooks` mechanism fires mid-redirect on this httpx version — this
was the one criterion the packet explicitly said not to weaken, and it
wasn't.

Follow-up finding filed, not a blocker: #1368 — the SSRF guard re-resolves
the hostname at hook-fire-time but doesn't pin the httpx connection to the
already-validated IP, so a precisely-timed DNS-rebinding attack could
theoretically slip through between check and connect. This is a
substantially more sophisticated attack than the SSRF-via-pasted-URL
threat model this packet addressed (the finding this todo pair was
scoped against), and closing it fully would need a custom httpx transport
— out of scope for this packet's spec, correctly not attempted inline.
No out-of-control triggers fired.

Stitched.
