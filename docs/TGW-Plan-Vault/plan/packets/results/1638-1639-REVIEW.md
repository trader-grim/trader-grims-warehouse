# Review: #1638 (app-code step) + #1639 nats_client.py fixes

status: cleared
reviewer: Claude (tgw-runner-review skill)
date: 2026-07-22

## Checked

- Packet specs: `docs/TGW-Plan-Vault/plan/packets/1638-nats-stream-single-authority.md`
  (app-code step 2 only — step 1/flake side out of scope for this branch,
  handled separately per invariant E12) and
  `docs/TGW-Plan-Vault/plan/packets/1639-tgw-health-nats-asyncio-fix.md`.
- Result manifest: `1638-1639-RESULT.md` — status/files-touched/live-evidence
  all present, no sanity-check trigger.
- Diff scope: `git diff catio-nix-0.0.1-alpha...todo/1638-1639-nats-client-fixes`
  (merge-base comparison, not branch-tip vs. current main which was
  misleading due to unrelated plan-doc churn landing on main after this
  branch was cut) — exactly 4 files: `src/tgw/apis/nats_client.py`, its
  test file, the result manifest, and the INPROGRESS breadcrumb. No
  out-of-scope files.
- `_ensure_streams()` diff matches spec verbatim: read-only `stream_info()`
  check per stream, `log.error()` (not `.warning()`) on miss, no
  `add_stream()` calls, stale `max_bytes -1` comment removed.
- `_run_isolated()` diff matches spec verbatim (thread + fresh loop +
  join), both `check_nats()`/`query_mutations()` switched over, outer
  `try/except` fallback shape unchanged, background-publisher thread
  machinery untouched as specified.
- New tests are additive only, scoped to the two functions the packets
  touch (`TestEnsureStreamsReadOnly`, `TestRunIsolated`) — no unrelated
  test-framework/fixture changes.
- Live evidence: real broker verification for both packets (zero
  `add_stream` calls confirmed via instrumented `js.add_stream`; both
  functions confirmed callable from inside a running event loop against
  the real broker) — not "tests pass" alone. Executor explicitly reasoned
  about the worktree/editable-install trap (systemd worker runs the
  shared checkout, not this branch) and used a live-broker script running
  this worktree's own code instead, rather than trusting a
  `systemctl restart` that would have silently validated the wrong copy.
- Full suite: 2756 passed, 2 pre-existing failures (confirmed pre-existing
  on base branch, unrelated file) filed as todo #1641 rather than fixed
  inline — correct, out of this packet's scope.
- No `invariants.md` entries specific to `nats_client.py` yet; E9
  (announce_script_run for the throwaway verify script) followed per the
  manifest.
- Deviations: none. Todo #1638/#1639 both marked done, `pp_ref` matches
  the packets (`PP-AIOPS-001`) on both.

## Summary

Clean. Matches spec exactly, correctly scoped, live-verified against a
real broker including the actual regression condition (nested event
loop). Cleared for stitch.
