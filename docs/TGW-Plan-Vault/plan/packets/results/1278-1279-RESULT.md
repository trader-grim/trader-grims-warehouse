# Result: #1278 / #1279 pm-intake-fetch-url-ssrf-sizecap
Status: done
Todo: #1278, #1279   PP: PP-COHESION-001
Files touched:
- src/tgw/workers/pm_intake.py
- tests/test_pm_intake.py

Live evidence:
- `fetch_url('http://127.0.0.1/')` -> `{'ok': False, 'url': 'http://127.0.0.1/', 'error': 'blocked: url targets a private/internal address'}` — pre-flight block, no network call attempted (verified both by direct call and by `test_fetch_url_blocks_loopback` asserting `httpx.Client` is never instantiated).
- `fetch_url('http://169.254.169.254/')` -> same blocked shape (cloud-metadata/link-local range).
- `_resolve_is_safe('localhost')` -> `False`, `_resolve_is_safe('1.1.1.1')` -> `True` (stdlib `socket.getaddrinfo` + `ipaddress` classification works offline, no external DNS needed for literal/loopback targets).
- `test_fetch_url_blocks_redirect_to_loopback` exercises a REAL `httpx.Client` (only the transport is swapped for `httpx.MockTransport`) so the `event_hooks={'request': [...]}` mid-redirect guard is proven against this installed httpx version (0.28.1), not just documented behavior: a 302 from a "public" host to `http://127.0.0.1:9/` is caught before the redirect is followed.
- `test_fetch_url_rejects_oversized_content_length` asserts the mock body iterator (`resp.iter_bytes`) is `assert_not_called()` when `Content-Length` exceeds the 5MB cap — proves the body is never buffered when the fast-path header check catches it.
- `test_fetch_url_aborts_streaming_body_over_cap_without_content_length` streams >5MB with no `Content-Length` header and confirms the abort still fires from the running-total check, not just the header fast-path.
- Full offline suite: `PYTHONPATH=/opt/TGW/var/worktrees/1278-1279-pm-intake-fetch-url-ssrf-sizecap/src:$PYTHONPATH python3 -m pytest -q` -> `2126 passed, 1 skipped, 1 warning` (pre-existing skip, unrelated). `tests/test_pm_intake.py` alone: `39 passed`.
- Confirmed testing the worktree's own copy, not the shared checkout: `tgw.workers.pm_intake.__file__` resolves to `/opt/TGW/var/worktrees/1278-1279-pm-intake-fetch-url-ssrf-sizecap/src/tgw/workers/pm_intake.py`.

Deviations from spec: none. Implementation follows the packet's spec exactly:
- `_resolve_is_safe()` added verbatim as specified (stdlib `socket`/`ipaddress` only).
- Pre-flight hostname check before `httpx.Client` creation, plus the same
  check wired into `event_hooks={'request': [...]}` for mid-redirect
  re-checking, via a narrow `_SSRFBlocked` exception caught in `fetch_url()`
  (the packet allowed "the same except or a narrow except for whatever
  exception type the raise produces" — used a dedicated exception class
  rather than reusing a stdlib one, kept narrow and local to this module).
- `client.get()` replaced with `client.stream('GET', ...)` as a context
  manager; `Content-Length` fast-path check (5MB `_MAX_RESPONSE_BYTES`)
  then incremental `iter_bytes()` accumulation with abort once the running
  total exceeds the cap, before any decoding; decode uses `resp.encoding`
  matching how buffered `resp.text` would decode.
- Existing tests in `tests/test_pm_intake.py` that mocked `client.get(...)`
  (`test_fetch_url_success`, `_http_error`, `_unsupported_content_type`,
  `_network_error`, `_plaintext`, and the two `test_handle_url_submission_*`
  tests via `_mock_httpx_ok`) were updated to mock `client.stream(...)`
  instead, since the internal HTTP call mechanism changed as the spec
  required — this was necessary to keep those pre-existing tests passing
  against the new implementation, not a scope expansion (still entirely
  within `fetch_url()`'s test coverage, the packet's own context-budget
  scope). These test-mock hostnames (`example.com` etc.) also needed the
  SSRF pre-flight check patched to bypass real DNS in an offline test run —
  added a small `_patch_resolve_safe()` test helper for this.

Out-of-scope findings filed: none. No adjacent issues encountered outside
this packet's scope (no operational friction hit either — worktree/venv
setup and PYTHONPATH override worked as documented in this profile's
instructions).
