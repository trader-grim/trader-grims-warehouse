# Result: 1313-1316 revision.py fence read/write fix
Status: done
Todo: #1313, #1316   PP: PP-COHESION-001
Files touched:
- src/tgw/revision.py (cmd_revise + cmd_revise_apply: sku_old-aware read via resolver.find_current_sku/load_item_doc; archive_root passed to atomic_write_json in both write call sites)
- tests/test_revision.py (new TestCmdReviseFenceCompliance class covering sku_old fallback + archive-on-overwrite for cmd_revise; new test_archives_pre_overwrite_content_on_live_apply in TestLiveApply for cmd_revise_apply)

Live evidence:
- `PYTHONPATH=/opt/TGW/var/worktrees/1313-1316-revision-fence-read-write/src LD_LIBRARY_PATH=$NIX_LD_LIBRARY_PATH pytest tests/test_revision.py -q` → `77 passed in 0.44s` (confirmed `tgw.revision.__file__` resolves under the worktree path before running, per contract).
- Full offline suite: `PYTHONPATH=<worktree>/src LD_LIBRARY_PATH=$NIX_LD_LIBRARY_PATH pytest -q` → `2180 passed, 1 skipped, 1 warning in 120.88s` — zero regressions.
- New test `test_resolves_old_sku_via_sku_old_fallback`: item written under new SKU `tgw002` with `sku_old: tgw001`; `cmd_revise(cfg, "tgw001", ...)` now resolves and succeeds (previously would 404 with "item JSON not found").
- New test `test_archives_pre_overwrite_content_on_write` (cmd_revise) and `test_archives_pre_overwrite_content_on_live_apply` (cmd_revise_apply, dry_run=False, mocked eBay client): both confirm a `.zip` archive appears under `cfg['archive_root']` after the write — the actual E5 regression check, not just "call succeeded".

Deviations from spec: none. Used module-level `from tgw.resolver import find_current_sku, load_item_doc` (revision.py had no prior module-level resolver import; packet's Spec text showed local imports as the literal replacement but explicitly said to prefer a module-level import "if so" it already existed — since none existed, module-level was chosen as the cleaner/consistent option applied identically to both functions, per the packet's own "consistently" instruction). Flagging this choice explicitly per the packet's own instruction to check whether module-level import is already present.

Out-of-scope findings filed: none — no adjacent issues found while confined to the two call sites the packet specified.
