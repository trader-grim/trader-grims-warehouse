# Result: #1638 (app-code step) + #1639 nats_client.py fixes
Status: done
Todo: #1638 (app-code step of), #1639   PP: PP-AIOPS-001

Files touched:
- src/tgw/apis/nats_client.py
- tests/test_nats_client.py (new tests added, no existing tests removed)
- docs/TGW-Plan-Vault/inbox/claude/INPROGRESS-1638-1639-nats-client-fixes.md (breadcrumb)

## #1638 (app-code step only — flake side out of scope, separate agent)

`_ensure_streams()` rewritten to be read-only: calls `stream_info()` for
`ITEMDATA_MUTATIONS` and `QUEUE_TRANSITIONS`; on a genuine miss, logs
`log.error()` (was previously an `add_stream()` call at `log.info`/
`log.warning` level) and continues without creating anything. The
`add_stream()` calls and the stale `"max_bytes -1 = server-side limit
from nats-server.conf"` comment (disproven per the packet's root-cause
writeup) are both removed entirely.

## #1639

Added `_run_isolated()` helper (dedicated thread + fresh event loop per
call) and switched both `check_nats()` and `query_mutations()` from a bare
`asyncio.run(...)` to `_run_isolated(...)`, per the packet's suggested
shape. Outer `try/except Exception as e: return {"ok": False, ...}`
fallback shape unchanged in both functions. Background-publisher thread
machinery (`_bg_thread_main`, `init_nats`, `_enqueue`, `publish_mutation`,
`publish_queue_transition`) untouched, as specified.

## Live evidence

**Real running NATS broker at `nats://127.0.0.1:4222`** (tgw-nats
process; `nats stream ls` shows both `ITEMDATA_MUTATIONS` and
`QUEUE_TRANSITIONS` already present, both still `max_bytes: -1` —
flake-side bounding (#1638 step 1, separate agent) had not landed at
verification time, exactly the "expected, not your bug" case the packet
called out).

1. **#1638 — no add_stream call:** ran `init_nats()` from this worktree's
   code against the real broker with `js.add_stream` instrumented to
   record calls. Result: `add_stream calls made: []` — confirmed zero
   `add_stream` invocations. (Both streams already existed from a prior
   run, so this also exercises the "stream present" branch cleanly;
   `_ensure_streams`'s "stream missing → log.error, no create" branch was
   verified separately via a unit test with a mocked `stream_info` that
   raises — see `TestEnsureStreamsReadOnly.
   test_ensure_streams_never_calls_add_stream_when_missing` in
   `tests/test_nats_client.py`, which also asserts the `log.error` fires.)
   Note on the packet's suggested verification via
   `systemctl restart tgw-worker@echo.service`: the running systemd
   worker uses the shared checkout's editable install
   (`/opt/TGW/src/trader-grims-warehouse/src`), not this worktree's
   branch — restarting it would not have exercised this diff at all
   (this branch isn't merged/deployed). Used a live-broker script running
   this worktree's actual code instead, which is stronger evidence of
   the code's real behavior than restarting a worker still running old
   code. `systemctl restart tgw-worker@echo.service` should be re-run
   as part of post-merge verification, once this branch is stitched in.

2. **#1639 — both functions safe from inside a running loop:** ran, from
   inside `asyncio.run(async def _main_loop(): ...)` (the exact FastMCP
   condition described in the packet), `nats_client.check_nats()` and
   `nats_client.query_mutations("tgw-doesnotexist-verify-script")`
   against the real broker. Both returned normally with no
   `RuntimeError`:
   ```
   check_nats() from running loop: {'ok': True, 'url': 'nats://127.0.0.1:4222',
     'latency_ms': 2.2, 'streams': [{'name': 'ITEMDATA_MUTATIONS', 'ok': True,
     'messages': 0}, {'name': 'QUEUE_TRANSITIONS', 'ok': True, 'messages': 0}]}
   query_mutations() from running loop: {'ok': True, 'sku':
     'tgw-doesnotexist-verify-script', 'count': 0, 'mutations': []}
   ```
   Also confirmed the CLI (no-running-loop) path still works, both via a
   direct call to `nats_client.check_nats()` outside any loop and via
   `tgw.health.check_nats(cfg)` (the real call path `tgw health` CLI
   uses):
   ```
   {"ok": true, "check": "nats", "detail": "connected (1.2ms)",
    "elapsed_ms": 115.4, "url": "nats://127.0.0.1:4222", "latency_ms": 1.2,
    "streams": [{"name": "ITEMDATA_MUTATIONS", "ok": true, "messages": 0},
    {"name": "QUEUE_TRANSITIONS", "ok": true, "messages": 0}], "warn": false}
   ```
   Did not reach the live MCP server itself (not running from this
   worktree/session) — the nested-running-loop reproduction above
   exercises the identical failure condition FastMCP's `tgw_health()`
   tool would hit (a coroutine calling into `check_nats()`/
   `query_mutations()` from a thread that already has
   `asyncio.get_running_loop()` return non-None), which is what the bug
   actually is.

The throwaway live-verification script (`verify_1638_1639.py`) called
`tgw.logging.announce_script_run()` at the top per invariant E9, and was
deleted after use — never committed (confirmed via `git status`
immediately before/after; only the 3 files listed above are staged).

Full offline test suite: `pytest -q` — 2756 passed, 4 skipped, 2 failed.
The 2 failures (`tests/test_invariant_c12_field_set_accessors.py::
test_no_new_direct_field_set_access_outside_accessors` and
`::test_allowlist_has_no_stale_entries`) are **pre-existing on the base
branch** (`catio-nix-0.0.1-alpha`, confirmed by running that one test
file against the unmodified shared checkout — same 2 failures, same
line numbers, entirely in `ai_identify.py` which this packet never
touches) — stale line-number allowlist drift, unrelated to this diff.
Filed as todo #1641 rather than fixed inline (out of this packet's file
scope). `tests/test_nats_client.py` and `tests/test_health_nats.py`
(the two files covering this diff) both pass clean: 18/18.

Deviations from spec: none. `_run_isolated()` implemented per the
packet's suggested shape verbatim (result dict + daemon thread + join).

Out-of-scope findings filed: todo #1641 (stale C12 test allowlist,
`ai_identify.py` line-number drift, pre-existing, unrelated to this
diff).
