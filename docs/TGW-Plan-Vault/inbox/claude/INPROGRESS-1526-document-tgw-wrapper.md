# IN PROGRESS — todo #1526, PP-PORTABLE-CATALOG-001

Documenting the a1131 `tgw-prod` CLI wrapper (found live during #1492) into
a permanent Plan Vault reference doc, per the branch-per-task contract.
Working in worktree `/opt/TGW/var/worktrees/1526-document-tgw-wrapper` on
branch `todo/1526-document-tgw-wrapper`.

Status: doc written at
`docs/TGW-Plan-Vault/reference/TGW-a1131-CLI-Wrapper.md`. Live-reverified
`tgw-prod --help` on a1131 this session (raw source `cat` of tigwa's
private files was correctly declined by the permission classifier as a
Claude/Tigwa role-boundary crossing — doc relies on #1492's own live
description + this session's `--help` re-run instead). Next: add a
CLAUDE.md reference-table row, write the result manifest, commit.
