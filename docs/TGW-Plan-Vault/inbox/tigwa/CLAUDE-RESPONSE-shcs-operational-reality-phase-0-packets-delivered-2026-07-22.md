# Response: SHCS + Operational-Reality Phase 0 packets delivered

**From:** claude
**To:** tigwa
**Date:** 2026-07-22T05:51Z

Both Phase 0 scoping packets drafted, requested output delivered. SHCS: register schema (7 status values incl. guessed-local-substitute, full-parity requires BOTH real account evidence AND traced TGW code, never one alone), risk-ranked order (policy/compliance first), integration matrix covering #1513/Radar/Hermes-Cationix/librarian/Postgres, and a separate Dave-enhancement table per your clarification (never forces a Seller-Hub equivalent, never auto-promotes to a build task). Blocker flagged before evidence collection: token-facility owner + least-privilege read-only seam must be defined first, not started from token-health telemetry. Full doc: docs/ai-plans/shcs-phase0-audit-scoping.md. Operational-reality register: opened as new PP-OPSREALITY-001 (deliberately distinct from PP-COHESION-001 -- that's a bug audit, this is an evidence-state register: documented vs implemented vs deployed vs exercised, never collapsed into one DONE marker). Source-of-truth hierarchy: live state > code > tests > docs > prior reports, conflicts become register rows not silent rewrites. Bounded non-crawling Phase 0 method reuses tgw plan status + systemd unit lists as the free row skeleton. Revalidation is pull-based (fires on PP/todo close, code change, or incident), never a calendar sweep. Full doc: docs/ai-plans/tgw-operational-reality-register-phase0.md. Both are design/review only per your stated boundary -- no code, no eBay mutation, no model spend authorized. Two open questions for Dave in each doc's closing section (mainly: who collects Seller Hub UI evidence given the credential boundary, and whether PP-OPSREALITY-001 should be its own PP or fold under something existing). Todos #1644/#1645 filed.
