# TIGWA REQUEST — todo #1347: supported Tigwa-lite → a1131 wake path

**From:** Tigwa  
**For:** Claude startup intake  
**Date:** 2026-07-12  
**PP:** PP-HERMES-EA-001  
**Existing tracker item:** #1347, assigned to `claude`, priority 12  
**Related Tigwa item:** #1346, blocked by #1347

Dave directed Tigwa to ask Claude for the supported wake setup because the TGW network and host arrangement contains nuances Tigwa should not guess. Dave has also directed Tigwa to use the plan inbox for Claude requests during the current training period. This note mirrors the already-existing tracker item; it does not create a second task.

## Requested outcome

Define and set up the supported Tigwa-lite → a1131 wake path using the existing PP-HERMES-EA-001 wake-trigger design. Provide Tigwa with a bounded, tested interface and operating contract that can later be integrated into #1346's dedicated Tigwa-lite Telegram incident channel.

## Required considerations

Please reconcile and document:

1. Authoritative a1131 host identity and Wake-on-LAN MAC.
2. Correct sending host, network interface, broadcast address/route, and any router/switch constraints.
3. Existing hostname-resolution issue: `tgw-prod` does not currently resolve from a1131; use the verified LAN IP `192.168.60.100` where appropriate until naming is fixed.
4. Where the wake command should execute and which user/service owns it.
5. Required package, executable path, sudo/polkit permissions, and service environment.
6. Preservation of the settled userspace-only Hermes decision; do not recouple Hermes itself to the flake merely for wake handling.
7. Idempotence when a1131 is already awake.
8. Retry, timeout, and backoff behavior.
9. Readiness stages and checks:
   - Host responds on the LAN
   - SSH is available
   - Required full-Tigwa service/session is available
   - Alert context can be handed off successfully
10. What Tigwa-lite reports to Dave while waking:
    - Wake requested
    - Host responding
    - Tigwa ready / handoff complete
    - Wake or readiness failure, with recommendation
11. Audit logging for trigger, command, retries, readiness, outcome, and elapsed time.
12. Authority boundary:
    - Tigwa is authorized to wake a1131 when appropriate.
    - Tigwa is not authorized to suspend, sleep, shut down, or power off a1131.
13. Reuse existing TGW tools and settled designs rather than inventing a parallel wake framework.

## Live acceptance

Demonstrate safe wake/readiness behavior or, if a real sleep/wake cycle would be disruptive, perform an operator-approved nondisruptive test proving the command, already-awake handling, readiness sequence, and failure reporting.

Leave Tigwa a clear interface contract containing:

- Exact supported invocation/tool
- Inputs and outputs
- Expected status states
- Timeouts/retries
- Log location
- Failure behavior
- Required service/user context
- Integration point for todo #1346

## Governance

Tigwa will not implement or test a generic Wake-on-LAN command independently while #1347 is pending. Claude should reconcile this note through normal inbox intake and update the existing tracker item rather than creating a duplicate.
