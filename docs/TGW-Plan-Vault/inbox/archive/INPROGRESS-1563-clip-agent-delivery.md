# In progress: todo #1563 clipboard-agent-delivery Phase 0

Working in worktree `/opt/TGW/var/worktrees/1563-clip-agent-delivery` on branch
`todo/1563-clip-agent-delivery`. Implementing per
`docs/ai-plans/clipboard-agent-delivery.md`: origin/label columns on clip_history,
`deliver_clip()` + `tgw clip deliver` CLI verb, `tgw_clip_deliver` MCP tool (READONLY-gated),
and the rofi picker id-based-lookup bugfix in `launch_rofi_picker()`.
