Session 48 wrap-up (2026-07-06) — audit#1143 dead-letter/atomic-write/multi_intake work.

## What was done
- Executed todo #1234 (self-rescheduling workers only reschedule on success —
  dead-lettered jobs silently ended the chain forever) and #1235 (6 non-atomic
  / no-archive-before-write sites) as tgw-packets.
- Ran /code-review; found #1234's fix only covered 2 of 8 self-rescheduling
  workers — fixed the remaining 6 as todo #1242.
- Fixed 7 more code-review findings as todo #1243 (data_scrub_magento error
  isolation, mark_failed row-None classification, itemdata_scrub sort_keys,
  atomic-write dedup, token-save dedup via new _token_io.py, ITEM_DATA_ROOT
  drift).
- Dave questioned multi_intake.py's fence-bypassing SKU-collision branch;
  investigated live (eBay pull, exhaustive photo-size search, git blame),
  confirmed it was unverified/redundant, removed it (todo #1244, filed
  retroactively after a code-review conventions pass caught the missing
  breadcrumb).
- Ran /code-review again; collapsed the 8 hand-copied _on_terminal_failure
  overrides into one generalized worker_base.QueueWorker default (signature
  introspection, auto-calls a no-arg self._reschedule); documented
  strip_fields()'s catalog_verified side effect (todo #1245).
- Filed the 4 remaining PLAUSIBLE findings as todo #1246 (deferred, Dave
  wants to process them later — not urgent, no live-data risk).
- Committed everything as 3efdaed, pushed to origin/catio-nix-0.0.1-alpha,
  confirmed it updates the existing open PR #8 (no duplicate PR created).
- Restarted all 19 active tgw-worker@* services (twice, after each round of
  fixes); tgw health clean except 3 pre-existing tracked warnings (backups,
  nats, ebay_sync_fallback/#1077) — unrelated to this session.

## What is still open
- Todo #1246: the 4 deferred PLAUSIBLE findings — Dave is holding off given
  usage/reset timing, wants to process at his convenience.
- PR #8 not yet reviewed/merged by Dave.
- Dave is manually checking the UI/system behavior post-changes before
  deciding next steps.

## Next step
No action needed from Claude unless Dave asks. When resumed: check todo
#1246 first (4 small, well-scoped fixes, no new review needed), then check
PR #8 status.
