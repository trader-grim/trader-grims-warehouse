# TGW platform runbooks — v7 (2026-09-06)

These are the current operational versions. Earlier versioned files remain
unchanged as historical evidence.

- [Three-repository boundary v3](three-repository-boundary-v3-20260815.md)
- [Shared application source access v4](shared-source-access-v4-20260815.md)
- [Plan vault shared group access v1](plan-vault-shared-access-v1-20260908.md) —
  the vault's `.git` must be `core.sharedRepository = group`; a root operation
  can leave `.git/objects` fanout dirs non-group-writable and break commits
  probabilistically (2026-09-08 incident). `tgw doctor`
  **`access.plan-vault-git`** now checks this (added 2026-09-08).
- [Full platform installation and recovery v3](full-platform-installation-v3-20260815.md)
- [Release code to tgw-prod v1](release-to-tgw-prod-v1-20260906.md) —
  **supersedes** Governed production procedures v1. The prod deploy is a
  keyless `tgw-release-install`; the only gate is telling the operator before
  the `current` symlink flips.
- [TGW actor host bootstrap v1](tgw-actor-host-bootstrap-v1.md)
- [Harness onboarding v1](harness-onboarding-v1-20260906.md)
- [Remediation commit workflow v1](remediation-commit-workflow-v1-20260815.md)
- [Repository recovery inventory v1](repository-recovery-inventory-v1-20260815.md)

Superseded, kept as history:
- Governed production procedures v1 (2026-08-21) — the W13–W18
  admission/preflight/signature apparatus. Per
  OPERATOR-PRINCIPLE-20260903-SIMPLICITY and
  AMENDMENT-20260905-LEAF-11-DECOUPLE-LIB-PROD-AND-DELETE-APPARATUS this is
  being removed, not maintained. `cut-mordac` (`8ce516f0f`) removed its
  `procedure_runner` layer; the residual `release_installer` admission gate is
  bypassed per the v1 release runbook above.

Application source, Plan, and the production flake remain three separate
repositories and authority domains.

## Known gaps (tracked)

- **GitHub publish automation** (Todo 2001): the `tgw-publish.timer` +
  `tgw-plan-git` broker + `tgw-plan` deploy key are checked in
  (`three-repository-boundary-v3`) but not yet installed — run
  `scripts/install-tgw-plan-publish prepare`, register the deploy key on
  `trader-grim/tgw-plan`, then `… activate`. Until then GitHub is a manual
  `tgw-source-git publish` + operator-credential vault push. `tgw doctor`
  `source.github-publish` shows the current lag.
- **Operator effect-envelope** (`access.operator-sudoers`, PP-ROLES-001 WU-9,
  Todo 1997): `config/environment/sudoers.d/tgw-operators` is checked in but not
  yet ratified — every operator root op is still a `sudo -u db sudo` two-hop.
  Group is `tgw-operators` at fixed GID 4000 (`groupadd -g 4000`).
